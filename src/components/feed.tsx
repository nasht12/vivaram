import { UserButton, useUser } from "@clerk/nextjs";
import Head from "next/head";
import Image from "next/image";
import Link from "next/link";
import { LoadingPage } from "~/components/loading";
import { api } from "~/utils/api";

export default function Feed() {

  const { user } = useUser();
  const hello = api.example.hello.useQuery({ text: "from tRPC" });
  const { data, isLoading } = api.posts.getAll.useQuery();

  if(!data || isLoading) return <LoadingPage />

  return (
    <>
      <Head>
        <title>Create T3 App</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
  <div className="flex p-10 pl-16 gap-10 ">
   {[...data]?.map(({post, author}) => (
    <div key={post.id} className = "w-80">
      <Link href={`/post/${post.id}`} key={post.id}>
    <div>
    <Image src="https://cdn.discordapp.com/attachments/416094051339862016/1100205709133615164/wenatchee_lake.JPG"
     alt="car!" width="480" height ="320" 
     quality={75}
     priority={true}/>
     </div>
  <div className="card-body p-5 overflow-hidden ">
    <h2 className="card-title">{post.title}</h2>
  </div>
      </Link>
    </div>
   ))}
  </div>
    </>
  );
}
